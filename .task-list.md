# ReNova — User & Products Integration Task List

**Branch:** `nico-database-integration`
**Goal:** Add user authentication, encrypted PII storage, products/orders database integration, and admin panel.

---

## Instructions

- Complete tasks **one at a time**, in order (top to bottom).
- After finishing a task:
  1. Check the box `[x]`.
  2. Write a commit message below the task.
  3. Run `git add .` then `git commit -m "<message>"` **only** for that task's changes.
- If a task needs config (e.g., `.env.local`), document what to fill in.

---

## Tasks

### Phase 1 — Foundation

- [x] **Task 1: Create AES-256 encryption utility (`src/lib/crypto.ts`)** ✅ VERIFIED
  - Server-side crypto helpers for PII fields.
  - Functions: `encrypt(text)` → `"iv:tag:ciphertext"` hex string, `decrypt(str)` → plaintext.
  - Requires `SERVER_ENCRYPTION_KEY=<64 hex chars>` in `.env.local`.

  **Commit:** `feat(lib): add AES-256-GCM encryption helpers for PII`

  _Verified: All checks pass. Encryption format correct, key validation works._

---

### Phase 2 — Authentication Backend

- [x] **Task 2: Create auth API routes** ✅ VERIFIED
  - `src/app/api/auth/register/route.ts` — POST email/password registration, save user to Firestore (encrypted phone/address).
  - `src/app/api/auth/login/route.ts` — POST login, auto-create user on first Google sign-in.
  - `src/app/api/users/me/route.ts` — PATCH user profile (encrypted PII), GET decrypted user data.

  **Commit:** `feat(api): add auth registration, login, and user profile endpoints with encrypted PII`

  _Verified: All routes work. Login route auto-creates Firestore doc on first sign-in._

---

### Phase 3 — Authentication Frontend

- [x] **Task 3: Create auth hook (`src/lib/auth.ts`)** ✅ VERIFIED + COMMITTED
  - `useUser()` hook with `onAuthStateChanged`.
  - Client-side helpers: `registerClient`, `loginClient`, `googleLoginClient`, `logoutClient`.

  **Commit:** `feat(lib): add useUser auth hook and client-side helpers`

  _Verified: All hooks and helpers work. Included in phase commit._
  - `useUser()` hook with `onAuthStateChanged`.
  - Client-side helpers: `registerClient`, `loginClient`, `googleLoginClient`, `logoutClient`.

  **Commit:** `feat(lib): add useUser auth hook and client-side helpers`

- [x] **Task 4: Create login & register pages** ✅ VERIFIED + COMMITTED
  - `src/app/login/page.tsx` — email/password + Google sign-in form.
  - `src/app/register/page.tsx` — registration with optional encrypted phone/address fields.

  **Commit:** `feat(ui): add login and register pages with Google sign-in`

---

### Phase 4 — Products Database

- [ ] **Task 5: Create products API routes**
  - `src/app/api/products/route.ts` — GET all products, POST create product (encrypted specs).
  - `src/app/api/products/[id]/route.ts` — GET single product, PATCH update, DELETE.

  **Commit:** `feat(api): add products CRUD endpoints with encrypted fields`

---

### Phase 5 — Orders Database

- [x] **Task 6: Create orders API route** ✅ VERIFIED + COMMITTED (lint fixes applied)
  - `src/app/api/orders/route.ts` — POST create order (encrypted courier phone/destination), GET user orders.

  **Commit:** `feat(api): add orders CRUD endpoints with encrypted PII`

---

### Phase 5 — Products Database

- [x] **Task 5: Create products API routes** ✅ VERIFIED + COMMITTED (lint fixes applied)
  - `src/app/api/products/route.ts` — GET all products, POST create product (encrypted specs).
  - `src/app/api/products/[id]/route.ts` — GET single product, PATCH update, DELETE.

  **Commit:** `feat(api): add products CRUD endpoints with encrypted fields`

  _Verified: All CRUD routes work. Auth guards still TODO as per original intent._

---

### Phase 6 — Admin Panel

- [ ] **Task 7: Create admin products management page**
  - `src/app/admin/productos/page.tsx` — list all products, add/edit/delete forms.
  - Fetches from `/api/products`, creates/updates via API calls.

  **Commit:** `feat(ui): add admin product management panel`

---

### Phase 7 — Database Integration

- [ ] **Task 8: Update existing pages to fetch from database**
  - `src/app/page.tsx` — fetch products from `/api/products` instead of static import.
  - `src/app/pedidos/page.tsx` — fetch orders from `/api/orders` instead of static data.

  **Commit:** `refactor: integrate dynamic data from Firestore via API routes`

---

### Phase 8 — Configuration

- [ ] **Task 9: Add firestore.rules and update .env.example**
  - `firestore.rules` — security rules for users/products/orders collections.
  - Update `.env.example` with `SERVER_ENCRYPTION_KEY` placeholder + instructions.

  **Commit:** `docs: add firestore security rules and env config docs`

---

### Phase 9 — Seed & Verify

- [ ] **Task 10: Seed database with initial product data**
  - Create a seed script to migrate static products → Firestore.
  - Test all endpoints work end-to-end.
  - Run `npm run lint` and `npm run build` to verify.

  **Commit:** `feat: seed firestore with default products and verify all endpoints`

---

## Checklist (for the implementer)

- [ ] All tasks checked off above
- [ ] `npm run lint` passes
- [ ] `npm run build` succeeds
- [ ] `.env.local` has real Firebase config + SERVER_ENCRYPTION_KEY
- [ ] Firestore rules deployed to console
- [ ] Test registration → login flow works
- [ ] Test admin product CRUD works
- [ ] Test order creation with encrypted fields works
