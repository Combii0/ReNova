export type Product = {
  name: string;
  store: string;
  price: string;
  before: string;
  tag: string;
  rating: string;
  image: string;
  tone: string;
  time?: string;
  expirationDate?: string;
};